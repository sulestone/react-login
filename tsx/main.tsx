import * as React from 'react';
import * as ReactDOM from 'react-dom';
import LoginContainer from './login/LoginMainContainer.tsx';

ReactDOM.render(<LoginContainer />, document.getElementById('login'));